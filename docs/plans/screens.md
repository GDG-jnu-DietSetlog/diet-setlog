# diet-setlog — 화면별 레이아웃 스펙

> 출처: Figma "프롭 팀"([[figma-design-url]] · fileKey `WgmQmy0thsZO3mJ5gcO5zH`, Page 1 / `완성`). **읽기 전용 — 수정 금지.**
> 토큰·공통 컴포넌트는 [design-system.md](./design-system.md), 플로우/범위는 [dietsetlog-wireframe-plan.md](./dietsetlog-wireframe-plan.md), API는 [api-db-design.md](./api-db-design.md).
> 좌표는 프레임(390×844) 기준 절대 x/y(px). 색/간격은 design-system.md 토큰 참조. 좌표→기기 스케일은 `flutter_screenutil`(designSize 390×844, [spec-lock §11](./spec-lock.md)).
> ⚠️ **확정 델타**: STEP2 나이 추가 · 기록작성 간식 칩 · 캘린더 친구추가 제거 · 피드 v1 포함은 [spec-lock §2](./spec-lock.md) 기준(디자인 추가 슬롯 포함). 아래 본문에 ⭐로 표기.

## 화면 ↔ Figma 노드 매핑
| 앱 화면(플랜) | Figma 프레임 | nodeId | API |
|---|---|---|---|
| ProfileSetup STEP1 (이름) | 프로필1 | `1:643` | (클라 draft) |
| ProfileSetup STEP2 (성별/키/몸무게) | 프로필2 | `1:36` | (클라 draft) |
| ProfileSetup STEP3 (목표/시작) | 프로필3 | `1:74` | `PUT /v1/me/profile` |
| Home (빈 친구 상태) | 홈1 | `1:121` | `GET /v1/home` |
| FriendSearch (친구 추가/검색) | 홈2/친구추가 | `1:556`·`19:1796`·`19:1883` | `GET /v1/friends/search`, follow/unfollow |
| FoodCapture (카메라) | 음식 업로드 | `1:177` | (촬영 → `POST /v1/food-analyses`) |
| Analyzing (AI 분석 로딩) | 음식 업로드 | `1:198` | `GET /v1/food-analyses/{id}` 폴링 |
| FoodRecordEdit (기록 작성) | 음식 업로드 | `1:230` | `POST /v1/food-records` |
| Upload 완료 (성공) | 업로드 완료 | `1:295` | (저장 후 확인) |
| Feed (친구 피드) | 피드 | `1:343` | `GET /v1/feed`, like/comment ([spec-lock §5.2](./spec-lock.md)) |
| Calendar 월간 | Frame 캘린더 | `19:675` | (날짜 탭) |
| Calendar 일별 요약 | Frame 캘린더 | `19:1562` | `GET /v1/calendar/daily-summary?date=` |

---

## 1. ProfileSetup STEP1 — 이름 (`1:643`)
온보딩 1/3. **이름** 입력 → "다음".
- 앱바 390×60 @y44, 타이틀 "프로필 설정", 뒤로 44×44 @x24.
- 진행바 @y100 h4: 활성 ⅓(`#1B68FF`, 우측 radius9) + 회색 2칸.
- 본문 x24 y160 w342: "STEP 1/3"(14 Bold `#1B68FF`) → "이름을 설정해주세요"(20 Bold `#000`) → 입력 필드(342×48 @y233, 활성 테두리 `#1B68FF`) → "0/50자" 카운터(우측, 14 Medium `#CACACB`).
- CTA "다음" 342×56 @x24 y730.
- **필드**: 이름(max 50자).

## 2. ProfileSetup STEP2 — 기본 정보 (`1:36`)
온보딩 2/3. **성별·키·현재 몸무게** → 권장 칼로리 계산용.
- 진행바 ⅔.
- 헤더 x24 y149: "STEP 2/3" / "기본 정보를\n알려주세요"(20 Bold) / "키와 몸무게로 하루 권장 칼로리를 계산해드릴게요"(14 Medium `#878789`).
- 입력 그룹 x24 y302 w342, 세로 gap **36**:
  - **성별** 라벨(14 Bold `#4D4E51`) + 토글 2칩(gap10). 선택 `#E9F2FF`/`#1B68FF`, 미선택 `#F4F4F5`/`#C5C5C6`. ⚠️ Figma 칩 라벨이 placeholder("반복되지 않음"/"매주") → 구현 시 **남성/여성**.
  - ⭐ **출생연도** 라벨 + 필드(342×48, 값 18 Bold + 단위 "년" 우측, 숫자 4자리 또는 연도 휠 피커) — **디자인 추가 슬롯**([spec-lock §3](./spec-lock.md), 보드 노트 #5). 위치는 성별 토글과 키 필드 사이.
  - **키** 라벨 + 필드(342×48, 값 18 Bold + 단위 "cm" 우측).
  - **현재 몸무게** 라벨 + 필드(단위 "kg").
- CTA "다음" 342×56 @y730.
- **필드**: 성별(택1), ⭐**출생연도(birthYear, BMR 계산용)**, 키(cm), 현재 몸무게(kg).

## 3. ProfileSetup STEP3 — 목표 (`1:74`)
온보딩 3/3. **목표 몸무게·목표 날짜** + AI 권장 칼로리 표시 → "시작하기".
- 진행바 전체 폭.
- 헤더 x24 y150: "STEP 3/3" / "목표를\n설정해주세요" / "무리하지 않는 목표가 오래가요."
- **목표 몸무게** 그룹 x24 y303: 라벨 + 배지("현재보다 -6.0kg" 12 SemiBold `#00741E`, bg `#D0FFE4` radius26) + 필드(값 "62" + "kg").
- **목표 날짜** 라벨 @y416 + 필드 @y445(값 "2026.09.30" + 캘린더 아이콘 24×24) + 헬퍼 "오늘부터 약 15주 후예요."(14 Medium `#CACACB`).
- **AI 카드** x24 y544 **342×135**, bg `#E9F2FF` radius12: AI 아이콘18 + "AI가 계산한 맞춤 목표"(14 SemiBold `#1B68FF`) / "1650"(34 Bold) + "kcal / 일"(14 `#6B6B6B`) / 설명 2줄(13 Medium `#45474B`).
- CTA "시작하기" 342×56 @y730.
- **필드**: 목표 몸무게(kg), 목표 날짜(date picker). 권장 kcal는 읽기 전용. → `PUT /v1/me/profile`.

## 4. Home — 빈 친구 상태 (`1:121`)
진짜 홈은 이 한 화면(빈 상태). 상단 흰색, y236부터 `#F7F7F8` 패널.
- 상태바. 섹션 "오늘 인증한 친구"(14 Bold @x24 y108) + 헤어라인 @y99.
- 아바타 행 @y138: 내 아바타 54×54(bg `#F4F4F5`, border `#D7D8DA`, radius27, 우하단 파란 + 배지 22×22 `#1B68FF`) + "나" 라벨(12 Medium `#D5D6E1`) + placeholder 아바타/이름 스켈레톤 2.
- 빈 상태 중앙: 일러스트 원 98×98(bg `#E9F2FF` radius49) → "함께할 친구를 찾아보세요"(20 Bold center @y463) → 부제 2줄(14 Medium `#878789`) → Primary "친구 추가하기" 158×50 @y568 → "나중에 할게요."(14 SemiBold `#C2C2C4`).
- Bottom nav @y760, **홈 탭 활성**.

## 5. FriendSearch — 친구 추가/검색 (`1:556` 외)
추천 친구 목록 + 검색 + 연락처. (보드의 "홈2" 3프레임은 모두 이 화면 변형.)
- 앱바 "친구 추가"(`19:1883`은 "친구 검색").
- 검색바 343×48 @x24 y104(placeholder "이름 또는 아이디 검색").
- 연락처 카드 342×58 @y164: bg `#E9F2FF` radius12, 파란 아이콘칩 38×38 + "연락처로 친구 찾기"(14 ExtraBold) / "아는 친구를 빠르게 추가해요"(12 Medium `#82878F`) + > 화살표.
- 섹션 "추천 친구"(14 Bold @y242).
- **친구 행** ×N, 피치 66px(@y280, 346, 412, 478): 아바타 46×46(이니셜, 색세트 design-system §1.5) + 이름(14 Bold) / "함께 아는 친구 N명"(12 Medium `#C7C8C9`) + 액션 버튼.
  - **추가**(미선택): 65×36 bg `#1B68FF` radius12, + 아이콘 + "추가"(14 Bold `#FFF`).
  - **추가됨**(선택): 81×36 흰 배경 border `#EFF0F1` radius12, 체크 + "추가됨"(14 Bold `#000`).
- 하단 CTA 342×56 @y730: "1명 추가하고 시작하기"(`1:556`) / "1명 추가하기"(`19:1796`) / "선택하기"(`19:1883`).
- **API 매핑**: 행 = `GET /v1/friends/search` 응답의 `users[]`, 액션 = `POST`/`DELETE /v1/friends/{id}/follow`. `selected` = "추가됨" 상태.

## 6. FoodCapture — 카메라 촬영 (`1:177`)
다크 UI. 음식 사진 촬영(1단계).
- 앱바 `#0C0C0D`, 타이틀 "음식 기록"(16 Bold 흰), 닫기 X 40×40 원형 `#2E2E2F` @x24.
- 배경 사진 + 오버레이 `rgba(0,0,0,0.43)`. 중앙 초점 가이드 270×270 radius30, 흰 2px 테두리.
- 안내 토스트 @y662 176×28: "접시 전체가 보이게 찍어주세요"(12 SemiBold 흰, bg `rgba(30,30,28,0.59)` radius26).
- 하단 컨트롤 @y727, gap 70: 갤러리 썸네일 53×53 / **셔터 82×82**(흰 링) / 카메라 전환 44×44 `#2E2E2F`.

## 7. Analyzing — AI 분석 로딩 (`1:198`)
촬영 사진 blur + 오버레이. 중앙 컬럼 top261 w342 gap14:
- AI 아이콘 배지 120×120 radius60(반투명 파랑) → 안쪽 80×80 AI 그라데이션.
- "AI가 음식을 분석하고 있어요"(20 Bold 흰) / "칼로리와 영양 성분을\n자동으로 계산하는 중이에요."(14 Medium `#C2C2C2`).
- 진행바 200×6 radius30(채움 흰 139px).
- 인식 태그 행 gap8: "닭가슴살 ✓"·"샐러드 ✓"(확정, bg `rgba(158,158,158,0.51)`) / "방울토마토"(미확정, 흐림).
- **API**: `GET /v1/food-analyses/{id}` 폴링(1~2s, 지수백오프).

## 8. FoodRecordEdit — 기록 작성 (`1:230`)
라이트 UI. AI 결과 확인·수정 후 "피드에 올리기".
- 앱바 "기록 작성", 뒤로 44×44.
- 음식 사진 풀폭(높이186) + 칼로리 칩 120×42 @x18 y234(AI 배지 + "420" 20 Bold + "kcal").
- 본문 카드 x18 y300 w352 gap2:
  - 헤더: "✓ AI 분석 완료"(12 SemiBold `#00991B`) / "수정"(13 SemiBold `#1B68FF`, 편집 진입).
  - 음식명 "닭가슴살 샐러드볼"(16 Bold).
  - **영양 3박스**(gap17, 각 103×56 border `#E0E0E2` radius12): 단백질 38g `#007D2F` / 탄수 32g `#1B68FF` / 지방 14g `#252525`.
  - **끼니** 라벨 + 칩 ⭐**4개**(아침/점심/저녁/**간식**, 103×40 radius12): 선택 bg `#D0FFE4` border `#00C321` 텍스트 `#007016`, 미선택 흰 배경 `#858588`. **간식 칩 = 디자인 추가 슬롯**([spec-lock §2-5](./spec-lock.md)) — `snack` 기록 생성 경로 확보용.
  - **메모** 라벨 + 입력박스(풀폭 h57 radius12 border `#E0E0E2`, ≤200자).
- CTA "피드에 올리기" 342×56 @y730.
- **편집 필드**: 음식명·탄단지(g)·끼니(택1, 4종)·메모. 총 kcal/영양은 "수정"으로 진입. → `POST /v1/food-records`. ✅ 메모는 계약에 추가됨([spec-lock §4·§5.3](./spec-lock.md)).

## 9. Upload 완료 — 저장 성공 (`1:295`)
배경 `#F7F7F8`.
- 성공 체크 배지 92×92 @y138(반투명 그린 → `#00C321` 흰 체크).
- "피드에 기록했어요!"(20 Bold center @y245) / "친구들이 좋아요와 응원을\n남길 수 있어요."(14 Medium `#878789`).
- **오늘 섭취 카드** 342×113 @x24 y352 radius12 border `#EFF0F1`: "오늘 섭취 칼로리" / "목표 1,650 kcal" / "1260"(34 Bold) + "kcal / 일" / "390 kcal 남음"(`#007016`) + 진행바 232×8(그라데이션).
- **방금 올린 글 카드** 342×78 @y479: 썸네일 54×54 + 음식명(14 SemiBold) / "방금 | 점심"(12 SemiBold `#C7C8C9`) + 하트/댓글 0.
- CTA "피드에서 보기" 342×56 @y694(홈 아이콘 + 텍스트) + "닫기"(16 Bold `#4B4C4F`).

## 10. Feed — 친구 피드 (`1:343`, 390×1381)
✅ **v1 범위 포함**([spec-lock §2-6·§5.2](./spec-lock.md)). API: `GET /v1/feed`(나∪팔로위 공개 글) · 좋아요 `POST/DELETE /v1/posts/{recordId}/like` · 댓글 `GET/POST /v1/posts/{recordId}/comments`. 인스타형 카드 리스트.
- 스토리 행 = 홈의 `friendsCertifiedToday`. 카드 = `FeedPost`(좋아요/댓글 수·`likedByMe`·미리보기 댓글 2개). 필터칩 = `?mealType=`.
- 상단: 로고 placeholder + 알림벨(mdi:bell) 24.
- "오늘 인증한 친구" 스토리 행 @y138: 내 프로필 + 친구 아바타 46×46(인증 링 **2px `#00B31D`**, gap16).
- 필터 칩 @y250 gap8: "전체"(선택 `#1B68FF` 흰)·아침·점심·저녁(흰 border `#E0E0E2`).
- **피드 카드** x24 y302 w342 gap16, border `#E0E0E2` radius12:
  - 헤더: 아바타40 + 이름(14 SemiBold) + 끼니 뱃지(radius26) + 시간(12 SemiBold `#C7C8C9`) + 더보기.
  - 음식 사진 풀폭 h280 + 칼로리 칩(좌하단).
  - 영양 막대 3개(단백질 `#01D225`/탄수 `#1B68FF`/지방 `#FDDA08`, 막대 h5 radius9).
  - 본문 제목(14 Bold) + 캡션(12 Medium `#515255`).
  - 액션 행: 하트(`tabler:heart-filled`)/댓글(`mynaui:chat`)/공유(`mi:send`) + 저장(우측). 카운트(14 SemiBold `#171717`).
  - 댓글 미리보기 + "댓글 N개 모두 보기"(12 Medium `#C7C8C9`).

## 11. Calendar — 월간 (`19:675`)
배경 `#F7F7F8`. 월 그리드 → 날짜 탭 시 일별 요약(12)로.
- 앱바 "캘린더".
- 연·월 선택 박스 @x25 y105 163×~44(border `#1B68FF` radius12): "2025년 5월" 18 Bold `#1B68FF` + caret. (`19:433` = 드롭다운 열린 변형.)
- ✅ 우상단 친구추가 아이콘 48×48 @y106 — **제거 확정**(미구현). 친구추가 팝업·타인 달력 보기도 미구현([spec-lock §2-7](./spec-lock.md)).
- 요일 헤더(일~토, 14 Bold `#070606`) + 헤어라인 @y208.
- 날짜 그리드 @x24 y211: 7열(피치 51), 행 gap 13. 셀 = 숫자 캡슐(h36 radius18) + 점.
  - 당월 `#000` / 전·다음달 `#686C72`. **선택 날짜**: 캡슐 bg `#1B68FF` 흰 텍스트.
  - **점 8×8**: 기록 있음 `#26CF51` / 없음 `#CACACB`.
- Bottom nav **캘린더 탭 활성**.

## 12. Calendar — 일별 섭취 요약 (`19:1562`)
선택 날짜 상세. (`31:2300` = 동일, 날짜 포맷만 다름.)
- 앱바 "오늘 섭취 요약"(16 Bold). 날짜 텍스트 "2025년 5월 24일 토요일"(16 SemiBold center @y124).
- **도넛 카드** @x23 y161 130×144(border `#1B68FF` radius10): 원형 진행 + 중앙 "70 %"(22 Bold `#1B68FF`).
- **요약 카드** @y161 212×144(border `#1B68FF` radius10): "섭취 1,210kcal"(값 14 Bold `#1B68FF`) / "목표 1,800kcal" + 구분선 + 매크로 칩 3개(56×56 border `#1B68FF` radius12: 단백질 38g `#00741E` / 탄수 32g `#4583FF` / 지방 14g `#000`).
- "끼니별 기록" 헤더(16 SemiBold @y355) + 타임라인 세로선 @x24.
- **끼니 행 ×4**(피치 85, @y389~): 타임라인 점 18 + 시각(10 Medium) + 카드(bg `#E9F2FF` radius12 283×65): 끼니명(12 Bold `#1B68FF`) + "자세히 보기" 칩(radius4) + kcal(12 SemiBold) / 음식 설명(15 Medium).
  - 데이터 예: 아침 08:12 318kcal / 점심 12:30 543kcal / 간식 04:15 200kcal / 저녁 06:30 149kcal.
- ⚠️ "자세히 보기" 칩의 상세 이동은 **이번 단계 범위 밖**([api-db-design §6](./api-db-design.md)).
- **API**: `GET /v1/calendar/daily-summary?date=` → `recordsByMeal`로 끼니 행 렌더.

---

## 부록: 미사용/중복·변형 프레임
- `19:1796`·`19:1883` = FriendSearch 변형(텍스트만 차이, §5).
- `19:433` = 월간 달력 + 연·월 드롭다운 열림.
- `19:891`·`19:1335` = 월간 달력 + 친구추가 팝업(✅ 제거 확정 — 미구현).
- `19:1118` = "김서진의 캘린더"(타인 달력 보기 — ✅ 미구현, spec-lock §2-7).
- `31:2300` = 일별 요약(날짜 포맷 변형).
